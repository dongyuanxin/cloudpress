#!/usr/bin/env node

const path = require("path");
const sitemap = require("nextjs-sitemap-generator");

sitemap({
    baseUrl: "https://xxoo521.com",
    pagesDirectory: path.join(__dirname, "..", "out"),
    targetDirectory: path.join(__dirname, "..", "out"),
});
